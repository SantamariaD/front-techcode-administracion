import { NumerosPositivosDirective } from './numeros-positivos.directive';

describe('NumerosPositivosDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosPositivosDirective();
    expect(directive).toBeTruthy();
  });
});
