import { FechaNacimientoDirective } from './fecha-nacimiento.directive';

describe('FechaNacimientoDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaNacimientoDirective();
    expect(directive).toBeTruthy();
  });
});
