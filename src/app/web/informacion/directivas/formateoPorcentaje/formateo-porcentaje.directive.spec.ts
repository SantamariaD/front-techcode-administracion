import { FormateoPorcentajeDirective } from './formateo-porcentaje.directive';

describe('FormateoPorcentajeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateoPorcentajeDirective();
    expect(directive).toBeTruthy();
  });
});
