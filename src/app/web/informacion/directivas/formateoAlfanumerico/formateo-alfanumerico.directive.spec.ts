import { FormateoAlfanumericoDirective } from './formateo-alfanumerico.directive';

describe('FormateoAlfanumericoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateoAlfanumericoDirective();
    expect(directive).toBeTruthy();
  });
});
