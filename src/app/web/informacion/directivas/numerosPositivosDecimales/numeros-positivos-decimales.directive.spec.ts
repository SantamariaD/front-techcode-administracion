import { NumerosPositivosDecimalesDirective } from './numeros-positivos-decimales.directive';

describe('NumerosPositivosDecimalesDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosPositivosDecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
