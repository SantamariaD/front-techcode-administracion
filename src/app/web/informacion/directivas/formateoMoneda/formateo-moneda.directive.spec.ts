import { FormateoMonedaDirective } from './formateo-moneda.directive';

describe('FormateoMonedaDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateoMonedaDirective();
    expect(directive).toBeTruthy();
  });
});
