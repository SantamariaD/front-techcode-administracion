import { FormateoDescripcionesDirective } from './formateo-descripciones.directive';

describe('FormateoDescripcionesDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateoDescripcionesDirective();
    expect(directive).toBeTruthy();
  });
});
