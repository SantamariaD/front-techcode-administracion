import { FormateoMonedaPipe } from './formateo-moneda.pipe';

describe('FormateoMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormateoMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
