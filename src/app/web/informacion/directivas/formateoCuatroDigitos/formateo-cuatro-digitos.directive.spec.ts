import { FormateoCuatroDigitosDirective } from './formateo-cuatro-digitos.directive';

describe('FormateoCuatroDigitosDirective', () => {
  it('should create an instance', () => {
    const directive = new FormateoCuatroDigitosDirective();
    expect(directive).toBeTruthy();
  });
});
