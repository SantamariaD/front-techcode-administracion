import { RfcDirective } from './rfc.directive';

describe('RfcDirective', () => {
  it('should create an instance', () => {
    const directive = new RfcDirective();
    expect(directive).toBeTruthy();
  });
});
