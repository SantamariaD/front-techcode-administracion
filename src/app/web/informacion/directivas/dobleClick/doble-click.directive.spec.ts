import { DobleClickDirective } from './doble-click.directive';

describe('DobleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DobleClickDirective();
    expect(directive).toBeTruthy();
  });
});
